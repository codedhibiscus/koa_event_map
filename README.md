# koa_event_map
Testing how to dynamically load in maps of events/places. This can include restaurants (which is the first thing I'm making). 

It should dynamically load in the following from external data:
- Map
- Sprites
- Event points
- Interactions
